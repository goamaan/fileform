import type { Metadata } from 'next';
import { DocumentPage } from '../../components/site-chrome';
export const metadata: Metadata = {title:'Free CLI guide',alternates:{canonical:'/docs'}};
export default function Docs(){return <DocumentPage eyebrow="FREE & OPEN SOURCE" title="CLI guide" intro="Inspect, convert and verify local files. No app licence or account required."><h2>Build from source</h2><p>Use a Mac with the toolchain and pack setup listed in the repository README.</p><pre><code>{'git clone https://github.com/goamaan/fileform-core.git\ncd fileform-core\nswift build -c release\n.build/release/fileform --help'}</code></pre><p>Examples assume <code>fileform</code> is on your PATH. Originals stay untouched; choose a new output path if one already exists.</p><h2>Inspect files</h2><pre><code>{'fileform inspect cover.png\nfileform capabilities'}</code></pre><h2>Fit an image under 10 MB</h2><pre><code>fileform fit cover.png --to jpeg --max-bytes 10000000</code></pre><p>1 MB = 1,000,000 bytes. The complete output must meet the limit. For transparent input converted to JPEG, choose <code>--background white</code> or <code>--background black</code>.</p><h2>Convert a flat table</h2><pre><code>fileform convert data.csv --to json --output data.json</code></pre><p>CSV/TSV cannot retain JSON value types. Nested JSON is unsupported.</p><h2>Split PDF pages</h2><pre><code>{'fileform pdf split document.pdf --every 2 --output page-pairs --json\nfileform pdf split document.pdf --ranges \'2-5;4,2,4\' --output selected-groups --json'}</code></pre><p>Choose regular groups or one-based ranges. Commas group pages; semicolons start another PDF. Order and repeats are retained. The complete folder is saved after verification. In the app, Split tools updates the preview; Undo restores the arrangement.</p><h2>Export PDF pages as images</h2><pre><code>{'fileform pdf pages --input document.pdf --output page-images --dpi 144 --to png\nfileform pdf pages --input document.pdf --pages 2,1,2 --output selected-pages --dpi 300 --to jpeg --quality 0.85'}</code></pre><p>One-based page numbers preserve order and repeats. Results go in one new folder. Requires the native worker built with the CLI, without a PDF pack. Text, forms and visible annotations become pixels.</p><h2>Extract embedded PDF images</h2><pre><code>{'fileform pdf extract-images --input document.pdf --pdf-pack ./Artifacts/PDFPack --output embedded-images --dry-run --json\nfileform pdf extract-images --input document.pdf --pages 1-3 --pdf-pack ./Artifacts/PDFPack --output embedded-images --json'}</code></pre><p>Requires the PDF pack and native worker. Dry run lists unique images, source pages and skip reasons. Eligible JPEG bytes are preserved; supported samples become PNG at their original dimensions. Reused images are saved once, with provenance, encoding results and checksums.</p><h2>Compress a PDF without changing image quality</h2><p>Build the pack and worker using the <a href="https://github.com/goamaan/fileform-core/blob/main/Documentation/PDF-optimization.md">PDF pack instructions</a>. Every page is retained. Signed, encrypted, annotated, form-based, outlined and tagged PDFs are unsupported.</p><pre><code>{'fileform compress document.pdf --to pdf --pdf-pack ./Artifacts/PDFPack --output smaller.pdf\nfileform fit document.pdf --to pdf --pdf-pack ./Artifacts/PDFPack --max-bytes 1000000 --output fitted.pdf'}</code></pre><p>If the complete PDF cannot fit, nothing is saved and your original remains intact.</p><h2>Reduce PDF image quality</h2><p>Recompress supported images while retaining every page, text, vectors and metadata. The optional dimension limit resizes embedded images, not whole pages.</p><pre><code>{'fileform pdf optimize document.pdf --output email.pdf --quality 0.8 --minimum-quality 0.5 --maximum-image-dimension 1920 --max-bytes 1000000 --pdf-pack ./Artifacts/PDFPack --json'}</code></pre><p>Each attempt starts from the originals and stops at your quality floor. The complete PDF must meet the byte limit or nothing is saved. Unsupported images stay unchanged, with reasons in the report. <code>compress</code> and <code>fit</code> remain lossless by default.</p><h2>More commands</h2><p>Media operations require the media pack and supported codecs. Check current syntax:</p><pre><code>{'fileform transform --help\nfileform setup --help\nfileform fit --help'}</code></pre><div className="document-links"><a href="https://github.com/goamaan/fileform-core#readme">Full engine README ↗</a><a href="/formats">Current formats and limits</a></div></DocumentPage>}
