import type {Metadata} from 'next';import {DocumentPage} from '../../components/site-chrome';
export const metadata:Metadata={title:'Release status',alternates:{canonical:'/releases'}};
export default function Releases(){return <DocumentPage title="Release status" intro="The public desktop release is being prepared."><h2>macOS reference app</h2><p>Image conversion and crop, PDF organization and compression, media conversion and trim, local text recognition, tables, direct media links and saved setups are implemented. See the format-specific limits.</p><h2>Cross-platform preview</h2><p>The Electron interface now uses a native Rust worker for CSV/TSV-to-JSON conversion. The remaining workflows are being migrated and tested for macOS and Windows.</p><div className="document-links"><a href="https://github.com/goamaan/fileform/releases">GitHub releases</a><a href="/formats">Formats and limits</a><a href="https://github.com/goamaan/fileform/blob/main/Documentation/ROADMAP.md">Roadmap</a></div></DocumentPage>}
