import type { Metadata } from 'next';
import { DocumentPage } from '../../components/site-chrome';
import inventory from '../../public/data/capabilities.json';
export const metadata: Metadata = {title:'Formats & actions',alternates:{canonical:'/formats'}};
const operationNames:Record<string,string> = {'file.convert':'Convert files','image.crop':'Crop images','pdf.compose':'Combine and arrange PDF pages','pdf.split':'Split PDFs','pdf.rasterize':'Export pages as images','pdf.extract-images':'Extract embedded PDF images','pdf.optimize':'Recompress PDF images','media.trim':'Trim audio or video','link.fetch':'Save a direct media link'};
const familyNames:Record<string,string> = {image:'Image',media:'Audio or video',pdf:'PDF',table:'Table',text:'Text'};
export default function Formats(){return <DocumentPage eyebrow="VERSIONED CAPABILITY SNAPSHOT" title="Formats & actions" intro="Current macOS reference-engine outputs and limits."><div className="policy-note">September 14, 2026 · Engine {inventory.engineVersion} · macOS 26.2 · Media and PDF packs included. Availability depends on your file, packs and macOS version. The Electron app is migrating these routes; Windows support is not yet verified.</div><p><a href="/data/capabilities.json" download>Download capability inventory</a></p>{Object.entries(operationNames).map(([id,name])=><section key={id}><h2>{name}</h2><div className="format-table-wrapper"><table className="format-table"><thead><tr><th>Start with</th><th>Output</th><th>What to know</th></tr></thead><tbody>{inventory.routes.filter(route=>route.operationID===id).map(route=><tr key={route.id}><td>{route.inputFamilies.length?route.inputFamilies.map(family=>familyNames[family]??family).join(' / '):'Direct media URL'}</td><td>{route.outputFormat==='images'?'JPEG / PNG':route.outputFormat.toUpperCase()}<br/><small>{route.available?'Available':'Unavailable'}{route.cardinality==='directory'?' · multiple files':''}</small></td><td>{route.limitation??route.verification}</td></tr>)}</tbody></table></div></section>)}<h2>Outside this build</h2><p>Broader image writers, Office/publishing formats, full PDF annotation/form/redaction editing and provider transformations are unavailable. Video-site page links are not direct media URLs.</p><h2>PDF assembly</h2><p>Selection, order, relative page boxes and rotation are checked after saving. The native writer may normalize coordinate origins, image interpolation hints and color spaces. Bookmarks, metadata and form behavior may change; existing signatures do not certify the new document.</p><h2>Page images</h2><p>Export all or selected pages as PNG/JPEG at 36–600 DPI in a new folder. Crops, rotations, visible annotations and filled fields are rendered. Oversized rasters are rejected. Selectable text and interactive forms become pixels.</p><h2>Embedded images</h2><p>Save each supported image object once at its original dimensions. Eligible JPEG bytes are preserved; supported 8-bit DeviceRGB/Gray samples become PNG, including supported soft-mask transparency. Requires the PDF pack and native worker.</p><p>Review counts and skip reasons first. Searches follow page resources and nested forms, which may include unused images. Inline images, annotation appearances and patterns are not enumerated. ICC-profiled, CMYK, Indexed and other unsupported encodings are reported. Whole-page rendering is never substituted.</p><h2>PDF compression</h2><p>Structural compression keeps every page and image quality. Signed, encrypted, annotated, form-based, outlined and tagged PDFs are unsupported. Image compression explicitly recompresses supported RGB/grayscale images as JPEG, with optional resizing. Up to six quality attempts stop at your chosen floor; unsupported images stay unchanged with a report. Text, vectors and metadata are checked. The complete PDF must meet a requested byte limit or nothing is saved.</p><h2>MP3</h2><p>Convert complete mono/stereo recordings at 32, 44.1 or 48 kHz, including audio from a single-track video. No implicit resampling or downmixing. Trim to WAV, FLAC or M4A first; direct MP3 trimming is unavailable.</p><h2>Properties that can change</h2><p>Image exports use sRGB and remove descriptive metadata. JPEG requires a background for transparency. Review OCR text and reading order. Flat-table conversion cannot retain every format’s value types.</p></DocumentPage>}
