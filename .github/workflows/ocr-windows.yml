name: Windows OCR evaluation
on:
  push:
    branches: [main]
    paths: ['Tools/build-ocr-evaluation.py', 'crates/fileform-engine/src/ocr*', 'crates/fileform-engine/src/native_pack.rs', 'crates/fileform-engine/tests/smoke-ocr*.py', 'crates/fileform-engine/tests/fixtures/ocr-page.ppm.gz', '.github/workflows/ocr-windows.yml']
  pull_request:
    paths: ['Tools/build-ocr-evaluation.py', 'crates/fileform-engine/src/ocr*', 'crates/fileform-engine/src/native_pack.rs', 'crates/fileform-engine/tests/smoke-ocr*.py', 'crates/fileform-engine/tests/fixtures/ocr-page.ppm.gz', '.github/workflows/ocr-windows.yml']
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: ocr-windows-${{ github.ref }}
  cancel-in-progress: false
jobs:
  ocr:
    runs-on: windows-2025
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Build pinned OCR sources and models
        shell: pwsh
        run: |
          python Tools/build-ocr-evaluation.py --work Artifacts/ocr-windows --jobs 4
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
      - name: Recognize the controlled raster fixture
        shell: pwsh
        run: |
          python crates/fileform-engine/tests/smoke-ocr-evaluation.py Artifacts/ocr-windows/OCRPack | Tee-Object -FilePath Artifacts/ocr-windows/smoke.txt
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
      - name: Verify pack integrity through the native engine
        shell: pwsh
        run: |
          rustup toolchain install 1.98.1 --profile minimal
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          $env:RUSTFLAGS='-C target-feature=+crt-static'
          cargo build --release --workspace --locked
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          python crates/fileform-engine/tests/smoke-ocr-pack.py Artifacts/ocr-windows/OCRPack | Tee-Object -FilePath Artifacts/ocr-windows/pack-smoke.txt
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          python crates/fileform-engine/tests/smoke-ocr-image.py Artifacts/ocr-windows/OCRPack | Tee-Object -FilePath Artifacts/ocr-windows/image-smoke.txt
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
      - name: Retain evaluation pack and build evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: fileform-ocr-evaluation-windows-x64
          path: |
            Artifacts/ocr-windows/OCRPack/
            Artifacts/ocr-windows/*.log
            Artifacts/ocr-windows/commands.json
            Artifacts/ocr-windows/smoke.txt
            Artifacts/ocr-windows/pack-smoke.txt
            Artifacts/ocr-windows/image-smoke.txt
          retention-days: 14
